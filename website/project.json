{
  "name": "website",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "website/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project website --web",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/dist/website"
      ],
      "options": {
        "command": "npx nx run website:build-base && cp -r dist/website/.next/* dist/website/ && rm -rf dist/website/.next dist/website/next.config.js dist/website/package.json dist/website/public dist/website/.nx-helpers",
        "parallel": false
      }
    },
    "build-base": {
      "executor": "@nx/next:build",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "root": "website",
        "outputPath": "dist/website",
        "nextConfig": "website/next.config.js"
      },
      "configurations": {
        "production": {}
      }
    }
  }
}
