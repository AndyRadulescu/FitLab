{
  "name": "website",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/website/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project website --web",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/dist/apps/website"
      ],
      "options": {
        "command": "npx nx run website:build-base && cp -r dist/apps/website/.next/* dist/apps/website/ && rm -rf dist/apps/website/.next dist/apps/website/next.config.js dist/apps/website/package.json dist/apps/website/public dist/apps/website/.nx-helpers",
        "parallel": false
      }
    },
    "build-base": {
      "executor": "@nx/next:build",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "root": "apps/website",
        "outputPath": "dist/apps/website",
        "nextConfig": "apps/website/next.config.js"
      },
      "configurations": {
        "production": {}
      }
    }
  }
}
